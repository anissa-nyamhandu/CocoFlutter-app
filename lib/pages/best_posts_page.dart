import 'package:flutter/material.dart';

class BestPostsPage extends StatelessWidget {
  const BestPostsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Future best posts page'),
      ),
    );
  }
}
